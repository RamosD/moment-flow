from django.apps import AppConfig


class IntegrationsBridgeConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.integrations_bridge"
    verbose_name = "Integrations Bridge"
