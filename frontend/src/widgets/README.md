# `widgets/` — Composed UI blocks

Self-contained sections of a page, assembled from features + entities + shared.

Slices: `app-shell`, `campaign-header`, `campaign-score-card`,
`campaign-recommendations-panel`, `campaign-moments-panel`,
`campaign-assets-panel`, `campaign-reports-panel`. Implemented from FE-010/FE-011.

May import from `features`, `entities` and `shared`.
