/**
 * Media kit generation renderer entry point.
 *
 * The real implementation lives in `media-kit-generation.renderer.ts` (CR-701).
 * This barrel keeps the dispatcher import path stable.
 */
export { renderMediaKitGeneration } from './media-kit-generation.renderer';
